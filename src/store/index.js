import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    // countryGeoIntegratedData: null,
  },
  mutations: {
    // countryGeoIntegratedData(state, payload) {
    //   state.countryGeoIntegratedData = payload;
    // }
  },
  actions: {
  },
  modules: {
  }
})
